# TODO add benchmarks for various schema complexities including
# - simple
# - highly nested (many $ref of $ref)
# - custom validators
# and benchmark that validates a 'vanilla' schema using jsonschema
# as a smoke test for slow downs in that key dependency
