Compression plugins are now required to implement both ``compress`` and ``decompress`` methods.
Plugins that only implement one or the other will continue to work but will generate a deprecation warning.
